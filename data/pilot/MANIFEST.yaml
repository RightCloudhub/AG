# P1-GOV-01 — pilot domain & corpus scope (synthetic engineering corpus)
# Generated for C1 scaffolding; replace with product-authorized real domain when ready.
version: "0.1.0"
status: draft  # draft | approved | superseded

domain:
  id: corporate_relations
  display_name: "公司关系 / Corporate Relations (synthetic pilot)"
  description: >
    Synthetic multi-hop corporate-relations corpus for G1→G2 C1 engineering
    validation: ownership, employment history, products, competition, supply,
    and M&A/event participation. Not a substitute for a product-signed real domain.
  owner_product: "TBD-product"
  owner_kg: "TBD-kg"

authorization:
  source: "synthetic_generated"
  license_or_agreement: "internal-synthetic-no-PII"
  approved_by: "engineering-scaffold"
  approved_date: "2026-07-19"
  expires_on: null
  pii_desensitized: true
  notes: >
    Documents generated by scripts/generate_pilot_corpus.py. All names and
    entities are fictional. Safe to use in CI/dev; do not treat as production
    domain lock for G2 effect gates without product re-signoff on real data.

corpus:
  raw_path: "data/pilot/raw"
  min_docs: 100
  doc_count: 226  # from scripts/generate_pilot_corpus.py + validate_pilot_corpus.sh
  format: "md"
  language: "en"
  approx_tokens: null

schema:
  path: "configs/schema/domain_v0.yaml"
  name: corporate_relations_v0
  notes: "Interim schema retained; revise when real pilot domain is locked"

multihop_focus:
  - "subsidiary → parent company → CEO → prior employers"
  - "product → producer → competitors"
  - "product → suppliers that also supply competitors"
  - "event participants → ownership chain (Apex / Meridian / Orion)"
  - "logistics dual-serve: BrightLink/Falcon serving peers"

checklist:
  domain_locked: false   # synthetic scaffold only; product must re-lock for real domain
  authorization_complete: true  # synthetic/no-PII path complete for engineering
  docs_ge_100: true
  ingest_smoke_ok: true  # validate_pilot_corpus.sh 226 docs → 226 chunks
  schema_aligned: true
  product_signoff: false
