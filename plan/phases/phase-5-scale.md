# 阶段五：规模化推广（持续）

**目标**：基于试点数据扩展领域与用户规模，建立长期图谱治理与 Agent 策略迭代机制。
**前提**：G4 通过；规模化按试点效果**另行立项**，本文件仅列方向性规划，不做详细排期。

## 工作方向

### 1. 领域扩展
- [ ] `P5-EXT-01` 第二、三领域图谱接入：复用抽取管线，按领域定义新 Schema；沉淀"新领域接入 Runbook"（目标：新领域从语料到可查询 ≤2 周）
- [ ] `P5-EXT-02` 跨领域实体对齐：同一实体在多领域图谱中的关联与消歧
- [x] `P5-EXT-03` 多租户配额管理脚手架 — `MultiLevelBudget` + auth principal（图库物理隔离仍待）

### 2. 能力增强（PRD P2 需求）
- [x] `P5-CAP-01` 图谱浏览 API 脚手架 — `GET /v1/graph/entities`（详情页 UI 待规模化立项）
- [x] `P5-CAP-02` 外部工具扩展框架 — `agent/tools/registry.py`（业务 handler 待注册）
- [x] `P5-CAP-03` 答案置信度分级 — `generation/confidence.py` · metadata.confidence
- [x] `P5-CAP-04` 学习型 Re-ranker 接口预留 — `retrieval/fusion.Reranker`（训练落地待数据）

### 2b. 前端框架化（rules.md §8 预留的阶段五事项）
- [x] `P5-UI-01` 试用 Web 前端框架化 — Vue 3 零构建重构 + 交互增强（ADR-006）；组件层/ CSS 三分拆/测试重写/docs 同步均完成；执行计划见 [p5-ui-01-vue-refactor.md](./p5-ui-01-vue-refactor.md)

### 3. 长期治理机制
- [ ] `P5-GOV-01` 图谱质量月度体检：抽取正确率、消歧准确率、覆盖度趋势报表
- [ ] `P5-GOV-02` 评测集持续扩充：badcase 与人工复核结论按季度回流评测集，防指标过拟合
- [ ] `P5-GOV-03` Agent 策略迭代机制：Prompt/分诊阈值/护栏参数变更须过评测集回归后灰度发布
- [ ] `P5-GOV-04` 成本优化持续项：模型降级实验、缓存命中率优化、图查询性能调优

### 4. 规模化立项输入（试点期收集）
- 试点期真实查询分布：简单/复杂比例、平均跳数、成本曲线
- 用户反馈主题聚类：最有价值的问题类型、最高频 badcase 归因
- 各领域扩展的 ROI 预估

## 成功度量（建议，立项时定稿）

| 维度 | 指标方向 |
|---|---|
| 规模 | 接入领域数、月活查询量 |
| 质量 | 线上负反馈率、评测集指标不回退 |
| 效率 | 新领域接入周期、单查询平均成本趋势 |
| 治理 | 审核队列处理时效、图谱抽检正确率 |
